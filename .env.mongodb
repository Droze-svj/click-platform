# MongoDB Configuration for Click
# Replace with your actual MongoDB Atlas connection string

MONGODB_URI=mongodb://localhost:27017/click
# For MongoDB Atlas, use:
# MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/click?retryWrites=true&w=majority

# Alternative: MongoDB Docker (if Docker is available)
# MONGODB_URI=mongodb://clickuser:clickpass123@localhost:27017/click
EOF && echo "✅ Configuration file created: .env.mongodb" && echo "" && echo "📄 INSTRUCTIONS FOR USER:" && echo "1. Set up MongoDB Atlas account and get connection string" && echo "2. Update MONGODB_URI in .env.mongodb" && echo "3. Copy the URI to server/.env or main .env file" && echo "4. Restart the backend server" && echo "" && echo "For immediate testing, let's try a local fallback approach..."