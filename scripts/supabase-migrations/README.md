# Supabase migrations

Run these in **Supabase Dashboard → SQL Editor** when instructed.

- **platform_accounts_multi_x.sql** — Allows multiple X (Twitter) accounts per user. Run this to enable "Add another X account" in Click. Until then, connecting a second X account will replace the first.
