# Audit Batch 3 (top-level A–M) — confirmed findings ledger

Exhaustive per-route audit, 103 files, **52 confirmed** (3-skeptic adversarial pass; verify partially rate-limited so some real findings may hide in the refuted set — re-verify pending). Fixed so far: approval kanban (#156), client-health (#157), probe set subscription/agency-campaigns/teams/referral (#155).

| # | file | sev | class | route | fix |
|---|------|-----|-------|-------|-----|
| 1 | agency-bulk.js | high | IDOR / broken object | POST /api/agency/:agencyWorkspaceId/bulk/clone-content (also | In each of the five handlers, before writing to a client workspace, validate the destination belongs to the agency via verifyClientWorkspaceAccess(agencyWorkspaceId, clientWorkspac |
| 2 | agency-bulk.js | low | pagination DoS / unb | POST /api/agency/:agencyWorkspaceId/bulk/customize-and-sched | Cap the array length before the loop, e.g. reject or slice with a MAX_BULK_CLIENTS constant (import clampInt from utils/pagination.js is for numeric limits; here add an explicit le |
| 3 | agency-business-enhanced.js | medium | crash-safety / pagin | POST POST /api/agencies/:agencyWorkspaceId/revenue/forecast  | Coerce and clamp months before use, e.g. `const months = clampInt(req.body.months, 6, 36, 1)` (utils/pagination.js) in each route (or inside the service). A sane max like 36/60 mon |
| 4 | agency-business-enhanced.js | low | NoSQL operator injec | GET GET /api/agencies/:agencyWorkspaceId/business-alerts | String()-cast the filter values before assigning: `if (severity) query['alert.severity'] = String(severity);` and `if (type) query['alert.type'] = String(type);` (or validate again |
| 5 | agency-business.js | low | NoSQL operator injec | GET /api/agencies/:agencyWorkspaceId/campaigns/cpa/metrics | String()-cast (and ideally enum-validate) the filter values before building the Mongo query in getCampaignCPAMetrics, e.g. `if (platform) query['campaign.platform'] = String(platfo |
| 6 | agency.js | high | IDOR / broken object | POST /api/agency/onboarding/:onboardingId/step | Before calling executeOnboardingStep, load the onboarding, and gate with checkPermission(req.user._id, onboarding.agencyWorkspaceId, 'canManageMembers') (same permission used by /o |
| 7 | agency.js | high | IDOR / broken object | POST /api/agency/reports/generate, /billing/track, /bulk-imp | After the agency checkPermission passes, verify the client belongs to the agency before touching it, e.g. in each service load the client with Workspace.findOne({ _id: clientWorksp |
| 8 | agency.js | low | pagination DoS | GET /api/agency/reports/latest | Use clampInt from utils/pagination.js: const limit = clampInt(req.query.limit, 10, 100, 1); before passing to getLatestAutonomousReports. |
| 9 | agentic.js | medium | IDOR / broken object | GET /api/agentic/status/:jobId | Scope the fetch by owner. Change getJobStatus to accept the caller id and enforce it: getJobStatus(jobId, String(req.user._id)) => AgenticJob.findOne({ jobId, userId: String(req.us |
| 10 | ai-content.js | critical | IDOR / broken object | POST POST /api/ai/templates | In the POST /templates handler, before calling the service: if req.body.templateId is present, load the template (AITemplate.findById) and require canAccessTemplate(template, req)  |
| 11 | ai-content.js | high | logic/correctness | POST POST /api/ai/templates | Before creating, call verifyWorkspaceAccess(req.user._id, req.body.agencyWorkspaceId) (from middleware/workspaceIsolation.js) and return 403 when !allowed; reject when agencyWorksp |
| 12 | approval-enhanced.js | critical | IDOR / broken object | POST /api/approvals/:approvalId/auto-advance and /api/approv | Before calling the service, load the approval scoped to the caller (e.g. ContentApproval.findOne({_id: approvalId, workspaceId: {$in: caller's workspaces}}) or use utils/ownership. |
| 13 | approval-enhanced.js | critical | IDOR / broken object | POST /api/clients/:clientWorkspaceId/kanban/bulk-move and /k | Add requireWorkspaceAccess('canEdit') on the workspace the caller actually controls and verify the target clientWorkspaceId/agency relationship server-side, OR have moveCard/bulkUp |
| 14 | approval-enhanced.js | high | IDOR / broken object | GET /api/clients/:clientWorkspaceId/kanban/filtered\|swimlan | Gate each route with requireWorkspaceAccess and resolve the authorized workspace from req.user, then pass only that verified workspace to the service; do not trust the URL clientWo |
| 15 | approval-enhanced.js | high | IDOR / broken object | GET /api/.../:entityId/visual-diff (GET) and /:entityId/anno | Resolve the parent Content/Post scoped to req.user._id / workspace (Model.findOne({_id: entityId, workspaceId/userId: ...})) before diffing/annotating, and 404 if the caller does n |
| 16 | approval-enhanced.js | medium | IDOR / broken object | POST /api/posts/:postId/comments/rich (POST) and /:postId/co | Verify the postId belongs to the caller's workspace (load Post scoped to req.user/workspace) before addRichComment; for reactions, confirm the parent post/comment is one the caller |
| 17 | approval-enhanced.js | medium | ReDoS | GET /api/workspaces/:workspaceId/comment-templates | Wrap search with utils/escapeRegex.js escapeRegex() before putting it in $regex (and/or cap its length), matching the pattern already used elsewhere in the codebase. |
| 18 | approval-kanban.js | critical | IDOR / broken object | POST POST /api/clients/:clientWorkspaceId/kanban/move (also  | Apply an authorization middleware to the router/route: use requireAgencyClientAccess (already imported-capable from middleware/workspaceIsolation.js) which verifies the user has ac |
| 19 | approval-kanban.js | high | IDOR / broken object | GET GET /api/clients/:clientWorkspaceId/kanban (also mounted | Protect the route with requireAgencyClientAccess (verifies user->agency membership and agency->client linkage) or requireWorkspaceAccess plus a client-belongs-to-agency check, befo |
| 20 | approval-workflow.js | high | IDOR / broken object | POST POST /api/approvals/:approvalId/comments | In the route (or inside addComment), fetch the approval scoped to the caller before mutating: ContentApproval.findOne({ _id: approvalId, $or: [ {createdBy: req.user._id}, {'assigne |
| 21 | approval-workflow.js | high | IDOR / broken object | POST POST /api/approvals/:approvalId/comments/:commentId/res | Load the approval scoped to req.user._id (participant $or on createdBy / assignedTo.userId / stages.approvals.approverId) and 404 when the caller is not a participant, before resol |
| 22 | approval-workflow.js | high | IDOR / broken object | POST POST /api/approvals/:approvalId/revisions | Scope the load to the caller as creator/participant (e.g. ContentApproval.findOne({_id, createdBy: req.user._id}) since only the creator re-submits, or the participant $or used els |
| 23 | approval-workflow.js | medium | missing/incorrect au | POST POST /api/approvals/multi-step | Add requireWorkspaceAccess() before the handler so the caller must be a member of workspaceId, and verify the content belongs to that workspace / the caller (e.g. Content.findOne({ |
| 24 | approvals.js | high | NoSQL operator injec | GET GET /api/approvals/workflows | String()-cast and validate teamId before use, and enforce team membership. In the route: `const teamId = req.query.teamId ? String(req.query.teamId) : null;` and reject non-ObjectI |
| 25 | approvals.js | medium | IDOR / broken object | GET GET /api/approvals/workflows | Before adding the `{ teamId }` branch, verify membership: load the Team and confirm req.user._id is in team.members (or an owner). Only then include teamId in the query. Reuse the  |
| 26 | audience.js | low | NoSQL operator injec | GET /api/audience/insights | String()-cast the value before it enters the filter: in the service set `query.platform = String(platform)` (or reject non-string `platform` in the route), or validate `platform` a |
| 27 | auth.js | high | logic/correctness | POST /api/auth/login | Add `last_login_attempt_at` to the SELECT column list at line 584 (and to the Mongo fallback shape if applicable) so line 654 reads the real timestamp; the lockout window then eval |
| 28 | autopilot.js | medium | pagination DoS | POST POST /api/autopilot | Cap the items array before building the plan. Use clampInt from utils/pagination.js on items.length (e.g. `const MAX = 500; const list = items.slice(0, clampInt(items.length, MAX,  |
| 29 | backup.js | medium | IDOR / cross-tenant  | POST /api/backup/preview (POST) and /api/backup/restore (POS | Scope the existence checks to the caller: replace Content.findById(item._id) with Content.findOne({ _id: item._id, userId }) (and likewise for ScheduledPost/Script) in backupServic |
| 30 | backup.js | medium | crash-safety / DoS ( | POST /api/backup/restore and /api/backup/preview | Add limits to the multer config: multer({ dest: 'uploads/backups/', limits: { fileSize: <e.g. 25 * 1024 * 1024> } }), matching the pattern used in video/advanced.js/manual-editing. |
| 31 | backup.js | low | crash-safety / temp- | POST /api/backup/restore and /api/backup/preview | Wrap cleanup in a finally block (or a try/finally around the handler body) that always does await fs.promises.unlink(req.file.path).catch(()=>{}) for both /restore and /preview, be |
| 32 | backup.js | low | logic/correctness /  | POST /api/backup/create | Add a per-user rate limiter to /create (and /export, which also calls createUserBackup), and enforce a retention cap (delete oldest backups beyond N) inside createUserBackup. |
| 33 | batch.js | high | mass-assignment | POST POST /api/batch/update | Sanitize `updates` before the $set: build the update via utils/safeUpdate.applySafeUpdates (or an explicit allowlist of user-editable fields such as title/description/category/isFa |
| 34 | benchmarking.js | medium | mass-assignment | POST POST /api/benchmarking/alerts | Never spread req.body over the owner. Build with an allowlist, e.g. `const alert = new BenchmarkAlert(); applySafeUpdates(alert, req.body, { allow: ['name','platform','metric','thr |
| 35 | benchmarking.js | medium | mass-assignment | PUT PUT /api/benchmarking/custom/:id | Replace `Object.assign(benchmark, updates)` with `applySafeUpdates(benchmark, updates, { allow: ['name','platform','metrics','isActive'] })` from utils/safeUpdate.js (userId is alr |
| 36 | client-guidelines.js | medium | mass-assignment | POST /api/workspaces/:workspaceId/guidelines | Do not spread req.body into the findOneAndUpdate update document. Build the update via the same guard used by the PUT route: strip identity/ownership fields with utils/safeUpdate.a |
| 37 | client-health-enhanced.js | critical | IDOR | POST POST /api/clients/:clientWorkspaceId/health-alerts/chec | Require requireAgencyClientAccess so both the agency membership and the client-belongs-to-agency link are verified before the service runs; derive agencyWorkspaceId from the verifi |
| 38 | client-health-enhanced.js | critical | IDOR | POST POST /api/clients/:clientWorkspaceId/key-wins/detect | Gate with requireAgencyClientAccess and derive workspaceId/agencyWorkspaceId from the verified req.agencyWorkspace/req.clientWorkspace context rather than trusting req.body. |
| 39 | client-health-enhanced.js | critical | IDOR | POST POST /api/competitors/:competitorId/sync | Fetch the competitor first, resolve its workspaceId, and verifyWorkspaceAccess(req.user._id, competitor.workspaceId) (or Model.findOne scoped to an accessible workspace) before mut |
| 40 | client-health-enhanced.js | high | IDOR | POST POST /api/clients/:clientWorkspaceId/health-score/forec | Add `requireAgencyClientAccess` (or resolve the workspace and call verifyWorkspaceAccess(req.user._id, clientWorkspaceId)) before the handler, OR scope the service query by the cal |
| 41 | client-health-enhanced.js | high | IDOR | GET GET /api/clients/:clientWorkspaceId/health-alerts | Gate with requireAgencyClientAccess (verify the caller's agency owns the client workspace), or add an agency/owner filter derived from req.user to the HealthAlert query. |
| 42 | client-health-enhanced.js | high | IDOR | GET GET /api/clients/:clientWorkspaceId/health/recommendatio | Add requireAgencyClientAccess before the handler (or verifyWorkspaceAccess(req.user._id, clientWorkspaceId)); do not act on an unverified path id. |
| 43 | client-health-enhanced.js | medium | auth | POST POST /api/competitors/sync-all | Restrict to an admin/internal role (requireAdmin) or remove the public route and run it only from the scheduler/cron; do not expose a global cross-tenant mutation behind plain auth |
| 44 | client-health.js | medium | missing/incorrect au | POST POST /:postId/comments/sentiment (also reachable as /ap | Add `requireWorkspaceAccess('canCreate')`-style scoping: first resolve the post's owning workspace (e.g. ScheduledPost.findById(postId) then verifyWorkspaceAccess(req.user._id, pos |
| 45 | client-health.js | low | pagination DoS | GET GET /:clientWorkspaceId/key-wins | In getKeyWins replace `.limit(parseInt(limit, 10))` with `.limit(clampInt(limit, 20, 500))` using utils/pagination.js clampInt. |
| 46 | client-health.js | low | NoSQL operator injec | GET GET /:clientWorkspaceId/key-wins and GET /:workspaceId/c | String()-cast (or enum-validate) each filter before assignment: `if (type) query['win.type'] = String(type);`, `if (impact) query['win.impact'] = String(impact);`, `if (platform) q |
| 47 | comments.js | low | NoSQL operator injec | GET /api/comments | String()-cast the value before it enters the filter, e.g. const eid = String(entityId); and query Comment.find({ teamId, entityId: eid }) in both the GET and POST handlers (and pas |
| 48 | content-insights-enhanced.js | high | IDOR / broken object | POST/GET /api/posts/:postId/predict, /:postId/predictions/up | Load the post first scoped to the caller: `const post = await ScheduledPost.findOne({ _id: postId, $or:[{userId:req.user._id},{workspaceId:...}] })` (or use utils/ownership.js guar |
| 49 | content-insights.js | critical | IDOR / broken object | POST POST /api/posts/:postId/video-metrics | Scope the write to the caller. Since this route has no workspaceId path param, look up the post with ownership in the service: `const post = await ScheduledPost.findOne({ _id: post |
| 50 | content-insights.js | high | IDOR / broken object | GET GET /api/posts/:postId/video-metrics/retention | Scope the read to the caller: fetch the owning ScheduledPost via `ScheduledPost.findOne({ _id: postId, userId: req.user._id })` (or filter VideoMetrics by the caller's workspaceId) |
| 51 | content-insights.js | high | IDOR / broken object | POST POST /api/posts/:postId/content-performance/update | Scope by owner: fetch `ScheduledPost.findOne({ _id: postId, userId: req.user._id })` (pass req.user._id into the service) or assertOwnership before recomputing; 403/404 when the po |
| 52 | content-insights.js | low | pagination DoS | GET GET /api/workspaces/:workspaceId/content/top-performing | Apply utils/pagination.js clampInt on limit before use, e.g. `const limit = clampInt(req.query.limit, 10, 100, 1);` and pass the clamped value to the service. |

## Fixed clusters
- **approval kanban** (approval-kanban.js, approval-enhanced.js client-workspace routes) — requireAgencyClientAccess — PR #156.
- **client-health** (client-health-enhanced.js 5 routes) — requireAgencyClientAccess/requireWorkspaceAccess — PR #157.

## Remaining high-priority clusters (to fix)
- **approval-enhanced.js approvalId/entityId routes** (auto-advance/route/delegate/visual-diff/annotations) + **approval-workflow.js** (comments/revisions) + **approvals.js** (NoSQL teamId) — approval→workspace scoping.
- **content-insights(.js/-enhanced.js)** — postId-scoped IDOR (video-metrics write/read, predictions, quality). Scope post to owner/workspace.
- **ai-content.js POST /templates** — canAccessTemplate on update + verifyWorkspaceAccess on create.
- **agency.js / agency-bulk.js / agency-business*.js** — clientWorkspaceId cross-tenant → verifyClientWorkspaceAccess.
- **auth.js** — login lockout no-op (missing column in SELECT) — brute-force protection defeated.
- **batch.js** — /batch/update mass-assignment (allowlist via applySafeUpdates).
- **client-health.js / audience.js / comments.js / agency-business.js** — NoSQL String() casts + pagination clamps.
- **backup.js** — cross-tenant integrity + crash/temp-file (medium).

Full detail (why/exploit/votes) in the audit run transcript.
