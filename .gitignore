node_modules/
client/node_modules/
# Environment files (sensitive)
.env
.env.local
.env.production
.env.staging
.env.render
.env.railway
.env.local.production
.env.mongodb
.env.monitoring.local
.env.test
.env.production.ready
.env.backup
.env.nosync
.env.local.nosync

# Common ignores
*.nosync
*.bak
uploads/
uploads_slow/
render-outputs/
dist/
build/
*.log
logs/
.DS_Store
.vscode/
.idea/

# Frameworks
.next/
node_modules/
.aider*

# Generated
ASSIGNMENT_READY.md

# Stray node_modules backups / scratch dirs
.node_modules_backup/
.node_modules-*
node_modules_slow/
_stray_*/

# Process / build / ffmpeg artifacts at root
*.pid
*_pid.txt
build_output.txt
client/build_log.txt
client/build_pid.txt
all-lint-errors.json
ffmpeg-*.log

# TS incremental-build cache (auto-regenerated)
*.tsbuildinfo

# Local scratch
scratch/
# Persistent local test DB (dev:test:server INMEMORY_DB_PATH) — never commit
.local-test-db/
output.mp4
debug-startup.js
error-catch-startup.js
debug_server.log

# Playwright test artifacts
playwright-report/
test-results/
ffmpeg-*.log
ffmpeg_logs/
backups/


# iCloud Drive conflict copies ("filename 2.ext", " 3", " 4") — this repo lives
# in iCloud, which creates these on sync conflicts. Never commit them.
* 2.*
* 3.*
* 4.*
* 2
* 3
* 4
# Legacy restored backup tree (removed from the repo)
Sovereign-Internal-Restored/
tests/reports/

# Claude Code harness — ephemeral session lock (do NOT track; contains pid/sessionId).
# Other .claude/ files (skills, settings, plans) stay tracked intentionally.
.claude/scheduled_tasks.lock
# Machine-local Claude permissions (absolute paths, per-machine) — keep out of the repo.
.claude/settings.local.json
.venv/
coverage/

.env.bak.*
